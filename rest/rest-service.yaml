apiVersion: v1
kind: Service
metadata:
  name: rest-service
spec:
  type: NodePort
  selector:
    app: rest
  ports:
    - protocol: TCP
      port: 5000  # Service port
      targetPort: 5000  # Container port
